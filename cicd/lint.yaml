workflow:run:
# required by ci job
- name: lint
  matrix:
    tool:
    - editorconfig-checker
    - shellcheck
    - yamllint
    - golangci-lint
  jobs:
  - cmd@http:presets?str|tpl#use-spec: tools/http.tpl
