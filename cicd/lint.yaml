workflow:run:
# required by ci job
- name: lint
  matrix:
    linter:
    - editorconfig-checker
    - shellcheck
    - yamllint
    - golangci-lint

  jobs:
  - shell@tpl|http:presets|tpl: linter/{{- matrix.linter -}}.tpl
    env:
    - name@tpl: |-
        {{- matrix.linter | strings.SnakeCase | strings.ToUpper -}}
      value@tpl: |-
        {{- matrix.version | default "" -}}
