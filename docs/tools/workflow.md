# workflow

## Supported Tasks

### Task `workflow:run`

Run jobs step by step

```yaml
workflow:run:
- name: example
  # jobs contains a list of actions, just the same as hooks but interruptable
  jobs: []
```
